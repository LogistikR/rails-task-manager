<div class="container">
  <h1>My Tasks</h1>
<% @tasks.each do | task | %>
    <ul>
      <li><%= link_to task.title, task_path(task)%></li>
    </ul>
</div>
<% end %>

<%= link_to "➕ Add a new tasks", new_path %>
